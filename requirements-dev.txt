# Development-only Python tooling. Runtime dependencies stay in app/api/requirements*.txt.
# Pinned exactly: a linter is a CI gate, and new ruff releases add rules to the
# selected rule sets (E/W/F/I/B/UP -- see pyproject.toml). A floating range means
# CI can start failing on a commit that changed nothing. Bump deliberately.
ruff==0.15.22

# Test runner + FastAPI TestClient transport (used by app/api/test_*.py)
pytest>=8,<9
httpx>=0.27
