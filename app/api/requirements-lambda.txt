# Trimmed runtime deps for the read-only Lambda image (deckgl-s3-cog-read).
#
# WHY a separate file: api/requirements.txt is shared with the Docker dev image
# and includes ingest-only deps (pyarrow, pillow, awscli). The read Lambda image
# installs this file plus boto3 and duckdb in Dockerfile.read, with DuckDB
# spatial/httpfs/aws extensions baked under /opt/duckdb_extensions.
fastapi==0.116.1
mangum==0.17.0
# pyproj + shapely: needed by /s1m/tiles (terrain coverage), served by this read
# API after the S1M consolidation. cover_tiles() lazy-imports pyproj (Albers<->4326
# reproject) and shapely (footprint rings) at request time, so both must be
# bundled or the endpoint 500s.
pyproj==3.7.2
shapely==2.1.1
